# Frontend

This folder is reserved for the user-facing app layer.

Current UI areas in the app root:
- app/
- components/
- lib/utils.ts

Keep route components, page layouts, client-side UI, and presentation logic here conceptually.
