# Backend

This folder is reserved for the server-side application layer.

Current backend areas in the app root:
- db/
- app/api/
- lib/auth.ts
- scripts/
- src/

Keep database access, server actions, API routes, session logic, and data seeding here conceptually.
